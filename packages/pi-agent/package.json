{
  "name": "@acr/pi-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Thin wrapper around pi-coding-agent: RPC client, runtime lifecycle, and project-scoped session management.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/picomatch": "^4.0.3",
    "@vitest/coverage-v8": "^3.0.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
