{
  "name": "@acr/pi-session",
  "version": "0.1.0",
  "private": true,
  "description": "Session metadata store for pi-coding-agent: list, resume, and archive JSONL sessions.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@acr/pi-agent": "workspace:*",
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
