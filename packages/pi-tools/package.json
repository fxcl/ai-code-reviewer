{
  "name": "@acr/pi-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Tool access control and sandbox policies for pi-coding-agent runtime.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@acr/pi-agent": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.5",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
